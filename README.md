Open transformer_lens.ipynb. Run all.

I stored all embedding matrices in a list - one embedding matrix for each transformer block in the llm.

The final code cell prints the learned unembedded output after each# tuned_lens_project
